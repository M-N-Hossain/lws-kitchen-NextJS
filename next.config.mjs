/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "plus.unsplash.com"], // Add unsplash as an allowed domain
  },
};

export default nextConfig;
